name: Release

on:
  push:
    tags: ['[0-9]*.[0-9]*.[0-9]*']
  workflow_dispatch:
    inputs:
      target_group:
        description: Target group to build
        required: true
        default: all
        type: choice
        options:
          - all
          - linux
          - freebsd
          - windows
          - macos

permissions:
  contents: read

concurrency:
  group: release-${{ github.event_name }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'workflow_dispatch' }}

env:
  CARGO_TERM_COLOR: always
  CARGO_HTTP_MULTIPLEXING: "false"
  CARGO_NET_RETRY: "10"
  CARGO_NET_GIT_FETCH_WITH_CLI: "true"
  KOSHELF_SKIP_NPM_INSTALL: "1"
  KOSHELF_SKIP_REACT_BUILD: "1"

jobs:
  preflight:
    name: Preflight
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        if: github.event_name == 'push'
        with:
          persist-credentials: false
      - name: Validate release tag
        if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/')
        run: |
          if [[ ! "$GITHUB_REF_NAME" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "Tag $GITHUB_REF_NAME is not a YEAR.MONTH.PATCH version" >&2
            exit 1
          fi
          version="$(sed -n '/^\[package\]/,/^\[/s/^version = "\([^"]*\)"/\1/p' Cargo.toml)"
          if [ "$version" != "$GITHUB_REF_NAME" ]; then
            echo "Tag $GITHUB_REF_NAME does not match package.version $version" >&2
            exit 1
          fi

  frontend:
    name: Frontend bundle
    needs: preflight
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - run: npm --prefix frontend ci
      - run: npm --prefix frontend run lint
      - if: ${{ !cancelled() }}
        run: npm --prefix frontend run format:check
      - if: ${{ !cancelled() }}
        run: npm --prefix frontend run build
      - name: Collect portable frontend build inputs
        run: bash .github/scripts/collect-frontend-build-assets.sh
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: frontend-build
          path: |
            frontend/dist
            frontend/build-assets
          if-no-files-found: error
          retention-days: 1

  linux:
    name: Linux ${{ matrix.platform }}
    needs: [preflight, frontend]
    if: github.event_name == 'push' || inputs.target_group == 'all' || inputs.target_group == 'linux'
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 45
    env:
      # riscv64 musl is a newer target that defaults to dynamic linking,
      # unlike the older musl targets; force the static binary the musl
      # artifacts promise. Target-specific, so every other row is untouched.
      CARGO_TARGET_RISCV64GC_UNKNOWN_LINUX_MUSL_RUSTFLAGS: -C target-feature=+crt-static
    strategy:
      fail-fast: true
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            platform: linux-gnu-x86_64
            runner: ubuntu-latest
            gnu: true
            x86_gnu: true
            glibc_floor: "2.17"
            cxxflags: ''
          # Build on the ARM runner so the smoke test runs on real hardware.
          # cargo-zigbuild pins the glibc floor independently of the host.
          - target: aarch64-unknown-linux-gnu
            platform: linux-gnu-aarch64
            runner: ubuntu-24.04-arm
            gnu: true
            glibc_floor: "2.17"
            cxxflags: ''
          - target: x86_64-unknown-linux-musl
            platform: linux-musl-x86_64
            runner: ubuntu-latest
            gnu: false
            cxxflags: ''
          - target: aarch64-unknown-linux-musl
            platform: linux-musl-aarch64
            runner: ubuntu-24.04-arm
            gnu: false
            cxxflags: ''
          - target: i686-unknown-linux-gnu
            platform: linux-gnu-i686
            runner: ubuntu-latest
            gnu: true
            host_multilib: true
            glibc_floor: "2.17"
            cxxflags: '-D__builtin_cpu_supports(x)=0'
          # GCC 13 ICEs while expanding debug locations in unrar's SSE
          # translation unit, as in ci.yml. The release profile has no C++
          # debug info today; -g0 keeps that from silently becoming required.
          - target: i686-unknown-linux-musl
            platform: linux-musl-i686
            runner: ubuntu-latest
            gnu: false
            host_multilib: true
            cxxflags: -g0
          # Hosted ARM runners cannot execute 32-bit ARM binaries, so smoke
          # test both ARMv7 slices under qemu-user instead.
          - target: armv7-unknown-linux-gnueabihf
            platform: linux-gnu-armv7
            runner: ubuntu-latest
            gnu: true
            arm_gnu: true
            host_multilib: true
            glibc_floor: "2.17"
            smoke_prefix: qemu-arm -L /usr/arm-linux-gnueabihf
            cxxflags: ''
          - target: armv7-unknown-linux-musleabihf
            platform: linux-musl-armv7
            runner: ubuntu-latest
            gnu: false
            host_multilib: true
            smoke_prefix: qemu-arm
            cxxflags: ''
          # No hosted runner can run riscv64, so smoke tests use qemu-user.
          # The glibc floor is 2.27 — the first glibc with riscv64 support.
          # Mainline LuaJIT has no riscv64 backend; mlua builds vendored PUC
          # Lua 5.1 on this arch instead (see Cargo.toml).
          - target: riscv64gc-unknown-linux-gnu
            platform: linux-gnu-riscv64
            runner: ubuntu-latest
            gnu: true
            glibc_floor: "2.27"
            smoke_prefix: qemu-riscv64 -L /usr/riscv64-linux-gnu
            cxxflags: ''
          - target: riscv64gc-unknown-linux-musl
            platform: linux-musl-riscv64
            runner: ubuntu-latest
            gnu: false
            smoke_prefix: qemu-riscv64
            cxxflags: ''
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: frontend-build
          path: frontend
      - uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772 # master
        with:
          toolchain: 1.96.0
          targets: ${{ matrix.target }}
      # LuaJIT's build-time code generator must have the same pointer width as
      # the target, so 32-bit targets need a runnable 32-bit host compiler.
      - name: Install 32-bit host compiler
        if: ${{ matrix.host_multilib }}
        run: |
          sudo dpkg --add-architecture i386
          sudo apt-get update
          sudo apt-get install --yes gcc-multilib linux-libc-dev:i386
      # Zig 0.16's ARM compiler_rt omits the 64-bit integer-to-float helpers
      # LuaJIT uses. Supply only GCC's static runtime; glibc still comes from
      # Zig and the resulting symbol floor is verified below.
      - name: Install ARM GCC runtime
        if: ${{ matrix.arm_gnu }}
        run: sudo apt-get install --yes libgcc-13-dev-armhf-cross
      - uses: taiki-e/setup-cross-toolchain-action@12b7ad4acfa95a1476779d6c06699b96ec1691f8 # v1
        if: ${{ !matrix.gnu }}
        with:
          target: ${{ matrix.target }}
      # Current stable Zig, tested by cargo-zigbuild in its own CI. Downloads
      # via the community mirror pool with minisign verification, as Zig asks
      # of CI automations.
      - uses: mlugg/setup-zig@d1434d08867e3ee9daa34448df10607b98908d29 # v2.2.1
        if: ${{ matrix.gnu }}
        with:
          version: 0.16.0
          cache-key: ${{ matrix.target }}
      - uses: taiki-e/install-action@67729d5c413db75907f0ad1e39bb04b9c868ff60 # v2.85.7
        with:
          tool: ${{ matrix.gnu && 'cargo-about@0.9.1,cargo-zigbuild@0.23.0' || 'cargo-about@0.9.1' }}
      - if: ${{ matrix.gnu }}
        run: |
          target_libs=-lunwind
          rustflags=
          if [ "$ARM_GNU" = "true" ]; then
            libgcc="$(find /usr/lib/gcc-cross/arm-linux-gnueabihf -name libgcc.a -print -quit)"
            if [ -z "$libgcc" ]; then
              echo "ARM libgcc.a was not found" >&2
              exit 1
            fi
            target_libs="$target_libs $libgcc"
            rustflags="-Clink-arg=$libgcc"
          fi
          if [ "$X86_GNU" = "true" ]; then
            libgcc="$(find /usr/lib/gcc/x86_64-linux-gnu -name libgcc.a -print -quit)"
            if [ -z "$libgcc" ]; then
              echo "x86_64 libgcc.a was not found" >&2
              exit 1
            fi
            target_libs="$target_libs $libgcc"
            rustflags="-Clink-arg=$libgcc"
          fi
          # An empty-but-set RUSTFLAGS would suppress every config-level
          # [target.*.rustflags]; only export it when there is something in it.
          if [ -n "$rustflags" ]; then
            export RUSTFLAGS="$rustflags"
          fi
          TARGET_LIBS="$target_libs" \
            cargo zigbuild --release --locked --target ${{ matrix.target }}.${{ matrix.glibc_floor }}
        env:
          ARM_GNU: ${{ matrix.arm_gnu }}
          X86_GNU: ${{ matrix.x86_gnu }}
          # Zig omits the __cpu_model runtime used by unrar's optional x86
          # dispatch. x86_64 restores it from GCC's static runtime; the i686
          # matrix value selects the scalar fallback instead.
          CXXFLAGS: ${{ matrix.cxxflags }}
          # The host strip cannot process a cross-compiled executable. This
          # executable is only a build byproduct; KoShelf is verified below.
          TARGET_STRIP: "true"
      - if: ${{ !matrix.gnu }}
        run: cargo build --release --locked
      - name: Verify binary
        env:
          GLIBC_FLOOR: ${{ matrix.glibc_floor }}
        run: |
          bin="target/${{ matrix.target }}/release/koshelf"
          file "$bin"
          # Capture readelf output first so a readelf failure fails the step;
          # inside `if readelf | grep -q`, only grep's exit status would count.
          if ${{ matrix.gnu }}; then
            syms="$(readelf --dyn-syms -W "$bin")"
            bad="$(grep -o 'GLIBC_[0-9.]*' <<< "$syms" | sort -uV \
              | awk -F_ -v fM="${GLIBC_FLOOR%%.*}" -v fm="${GLIBC_FLOOR#*.}" \
                '{ split($2, v, "."); if (v[1] > fM || (v[1] == fM && v[2] > fm)) print }')"
            if [ -n "$bad" ]; then
              echo "glibc symbols above $GLIBC_FLOOR: $bad" >&2
              exit 1
            fi
          else
            headers="$(readelf -lW "$bin")"
            dynamic="$(readelf -dW "$bin")"
            if grep -q ' INTERP ' <<< "$headers"; then
              echo "musl binary has a dynamic interpreter" >&2
              exit 1
            fi
            if grep -q '(NEEDED)' <<< "$dynamic"; then
              echo "musl binary has a shared-library dependency" >&2
              exit 1
            fi
          fi
      # The i686 GNU binary runs natively once the runner has a 32-bit glibc.
      - name: Install 32-bit glibc runtime
        if: matrix.target == 'i686-unknown-linux-gnu'
        run: |
          sudo dpkg --add-architecture i386
          sudo apt-get update
          sudo apt-get install --yes libc6:i386
      - name: Install qemu-user and ARMv7 glibc sysroot
        if: startsWith(matrix.target, 'armv7')
        run: |
          sudo apt-get update
          sudo apt-get install --yes qemu-user libc6-armhf-cross
      - name: Install qemu-user and riscv64 glibc sysroot
        if: startsWith(matrix.target, 'riscv64')
        run: |
          sudo apt-get update
          sudo apt-get install --yes qemu-user libc6-riscv64-cross
      # LD_BIND_NOW checks the complete symbol chain for dynamically linked
      # GNU builds. qemu passes it through to the ARMv7 guest loader.
      - name: Smoke test
        run: LD_BIND_NOW=1 ${{ matrix.smoke_prefix }} "target/${{ matrix.target }}/release/koshelf" --version
      - name: Package
        run: |
          install -d dist
          (cd "target/${{ matrix.target }}/release" && zip - koshelf) > "dist/${{ matrix.platform }}.zip"
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          path: dist/${{ matrix.platform }}.zip
          if-no-files-found: error
          archive: false
          retention-days: 1

  # FreeBSD, cross-compiled with cargo-zigbuild: zig ships the FreeBSD 14+
  # libc stubs and headers, so no BSD host is needed to build. aarch64 is a
  # Tier 3 target with no rustup std, so that slice builds std from source on
  # a pinned nightly; bumping that pin is a deliberate act like the 1.96.0 pin.
  freebsd:
    name: FreeBSD ${{ matrix.platform }}
    needs: [preflight, frontend]
    if: github.event_name == 'push' || inputs.target_group == 'all' || inputs.target_group == 'freebsd'
    runs-on: ubuntu-latest
    timeout-minutes: 45
    env:
      # Each row's matrix toolchain must win even if a rust-toolchain.toml
      # ever appears; stable cargo would reject the nightly row's -Z.
      RUSTUP_TOOLCHAIN: ${{ matrix.toolchain }}
    strategy:
      fail-fast: true
      matrix:
        include:
          - target: x86_64-unknown-freebsd
            platform: freebsd-x86_64
            toolchain: 1.96.0
            machine: Advanced Micro Devices X86-64
            # Zig omits the __cpu_model runtime used by unrar's optional x86
            # dispatch; select the scalar fallback like the i686 Linux rows.
            cxxflags: '-D__builtin_cpu_supports(x)=0'
          - target: aarch64-unknown-freebsd
            platform: freebsd-aarch64
            toolchain: nightly-2026-07-15
            build_std: true
            machine: AArch64
            cxxflags: ''
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: frontend-build
          path: frontend
      # Tier 3 has no rustup target to add; build-std needs rust-src instead.
      - uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772 # master
        with:
          toolchain: ${{ matrix.toolchain }}
          targets: ${{ matrix.build_std && '' || matrix.target }}
          components: ${{ matrix.build_std && 'rust-src' || '' }}
      # Current stable Zig, tested by cargo-zigbuild in its own CI.
      - uses: mlugg/setup-zig@d1434d08867e3ee9daa34448df10607b98908d29 # v2.2.1
        with:
          version: 0.16.0
          cache-key: ${{ matrix.target }}
      - uses: taiki-e/install-action@67729d5c413db75907f0ad1e39bb04b9c868ff60 # v2.85.7
        with:
          tool: cargo-about@0.9.1,cargo-zigbuild@0.23.0
      - if: ${{ !matrix.build_std }}
        run: cargo zigbuild --release --locked --target ${{ matrix.target }}
        env:
          CXXFLAGS: ${{ matrix.cxxflags }}
          TARGET_LIBS: -lunwind
          # The host strip cannot process a cross-compiled executable. This
          # executable is only a build byproduct; KoShelf is verified below.
          TARGET_STRIP: "true"
      # std comes from the nightly's vendored sources; Cargo.lock still
      # governs the crate graph.
      - if: ${{ matrix.build_std }}
        run: cargo zigbuild --release --locked --target ${{ matrix.target }} -Z build-std=std,panic_abort
        env:
          CXXFLAGS: ${{ matrix.cxxflags }}
          TARGET_LIBS: -lunwind
          TARGET_STRIP: "true"
      # zig brands FreeBSD binaries via the .note.tag FreeBSD notes and the
      # FreeBSD dynamic linker path, not the e_ident OS/ABI byte — the kernel
      # accepts either, so check what is actually there. No runtime smoke
      # test: qemu-user has no FreeBSD mode and a full VM outweighs a
      # --version check, so these slices stop at the static checks.
      - name: Verify FreeBSD ABI
        run: |
          bin="target/${{ matrix.target }}/release/koshelf"
          file "$bin"
          header="$(readelf -h "$bin")"
          grep -Eq 'Machine:[[:space:]]+${{ matrix.machine }}' <<< "$header" || {
            echo "binary is not ${{ matrix.machine }}" >&2
            exit 1
          }
          progs="$(readelf -lW "$bin")"
          grep -q 'interpreter: /libexec/ld-elf\.so\.1' <<< "$progs" || {
            echo "binary does not request the FreeBSD dynamic linker" >&2
            exit 1
          }
          notes="$(readelf -n "$bin")"
          grep -q 'FreeBSD' <<< "$notes" || {
            echo "binary has no FreeBSD ELF branding note" >&2
            exit 1
          }
      - name: Package
        run: |
          install -d dist
          (cd "target/${{ matrix.target }}/release" && zip - koshelf) > "dist/${{ matrix.platform }}.zip"
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          path: dist/${{ matrix.platform }}.zip
          if-no-files-found: error
          archive: false
          retention-days: 1

  windows:
    name: Windows ${{ matrix.platform }}
    needs: [preflight, frontend]
    if: github.event_name == 'push' || inputs.target_group == 'all' || inputs.target_group == 'windows'
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 45
    strategy:
      fail-fast: true
      matrix:
        include:
          - target: x86_64-pc-windows-msvc
            platform: windows-msvc-x86_64
            runner: windows-latest
            msvc_arch: x64
          - target: aarch64-pc-windows-msvc
            platform: windows-msvc-aarch64
            runner: windows-11-arm
            msvc_arch: arm64
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: frontend-build
          path: frontend
      - uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772 # master
        with:
          toolchain: 1.96.0
          targets: ${{ matrix.target }}
      - uses: taiki-e/install-action@67729d5c413db75907f0ad1e39bb04b9c868ff60 # v2.85.7
        with:
          tool: cargo-about@0.9.1
      # LuaJIT's MSVC build script uses this variable to select its ARM64 VM.
      # Native GitHub Windows runners do not enter a Developer Command Prompt.
      - run: cargo build --release --locked --target ${{ matrix.target }}
        env:
          VSCMD_ARG_TGT_ARCH: ${{ matrix.msvc_arch }}
      - name: Smoke test
        run: target/${{ matrix.target }}/release/koshelf.exe --version
      - name: Package
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path dist | Out-Null
          Compress-Archive -Path target/${{ matrix.target }}/release/koshelf.exe -DestinationPath dist/${{ matrix.platform }}.zip
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          path: dist/${{ matrix.platform }}.zip
          if-no-files-found: error
          archive: false
          retention-days: 1

  macos:
    name: macOS ${{ matrix.platform }}
    needs: [preflight, frontend]
    if: github.event_name == 'push' || inputs.target_group == 'all' || inputs.target_group == 'macos'
    runs-on: macos-latest
    timeout-minutes: 90
    environment: release
    strategy:
      fail-fast: true
      matrix:
        include:
          - target: x86_64-apple-darwin
            platform: apple-darwin-x86_64
            task: notarize-macos-intel
            min_macos: "10.12"
          - target: aarch64-apple-darwin
            platform: apple-darwin-aarch64
            task: notarize-macos-silicon
            min_macos: "11.0"
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: frontend-build
          path: frontend
      - uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772 # master
        with:
          toolchain: 1.96.0
          targets: ${{ matrix.target }}
      - name: Import Developer ID certificate
        uses: apple-actions/import-codesign-certs@5142e029c445c10ffc7149d172e540235a065466 # v7.0.0
        with:
          p12-file-base64: ${{ secrets.APPLE_CERT_P12_BASE64 }}
          p12-password: ${{ secrets.APPLE_CERT_PASSWORD }}
      - name: Configure signing and notarization
        env:
          APPLE_DEVELOPER_ID: ${{ secrets.APPLE_DEVELOPER_ID }}
          APPLE_USERNAME: ${{ secrets.APPLE_USERNAME }}
          APPLE_PASSWORD: ${{ secrets.APPLE_PASSWORD }}
        run: |
          team_id="$(printf '%s' "$APPLE_DEVELOPER_ID" | sed -n 's/.*(\(.*\)).*/\1/p')"
          if [ -z "$team_id" ]; then
            echo "APPLE_DEVELOPER_ID does not contain a Team ID" >&2
            exit 1
          fi
          umask 077
          {
            printf 'APPLE_DEVELOPER_ID="%s"\n' "$APPLE_DEVELOPER_ID"
            printf 'APPLE_NOTARY_PROFILE="koshelf-notary"\n'
          } > ./.env
          xcrun notarytool store-credentials koshelf-notary \
            --apple-id "$APPLE_USERNAME" --password "$APPLE_PASSWORD" --team-id "$team_id" >/dev/null
      - uses: taiki-e/install-action@67729d5c413db75907f0ad1e39bb04b9c868ff60 # v2.85.7
        with:
          tool: cargo-make@0.37.24,cargo-about@0.9.1
      - run: cargo make ${{ matrix.task }}
      - name: Verify minimum macOS version
        env:
          TARGET: ${{ matrix.target }}
          MIN_MACOS: ${{ matrix.min_macos }}
        run: |
          bin="target/$TARGET/release/koshelf"
          build="$(xcrun vtool -show-build "$bin")"
          if ! awk -v minimum="$MIN_MACOS" \
              '($1 == "minos" || $1 == "version") && $2 == minimum { found = 1 } END { exit !found }' \
              <<< "$build"; then
            echo "$bin does not declare macOS $MIN_MACOS" >&2
            printf '%s\n' "$build" >&2
            exit 1
          fi
      - name: Verify signature
        run: codesign --verify --strict --verbose=2 target/${{ matrix.target }}/release/koshelf
      # The ARM64 runner executes its native slice directly and the Intel slice
      # through Rosetta.
      - name: Smoke test
        run: target/${{ matrix.target }}/release/koshelf --version
      - name: Remove credentials
        if: always()
        run: rm -f ./.env
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          path: release/${{ matrix.platform }}.zip
          if-no-files-found: error
          archive: false
          retention-days: 1

  publish:
    name: Publish release
    needs: [linux, freebsd, windows, macos]
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/')
    runs-on: ubuntu-latest
    timeout-minutes: 15
    environment: release
    permissions:
      contents: write
      id-token: write
      attestations: write
    steps:
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          pattern: '*.zip'
          path: assets
          merge-multiple: true
          skip-decompress: true
      - name: Validate artifact set
        working-directory: assets
        run: |
          expected="$(printf '%s\n' \
            apple-darwin-aarch64.zip \
            apple-darwin-x86_64.zip \
            freebsd-aarch64.zip \
            freebsd-x86_64.zip \
            linux-gnu-aarch64.zip \
            linux-gnu-armv7.zip \
            linux-gnu-i686.zip \
            linux-gnu-riscv64.zip \
            linux-gnu-x86_64.zip \
            linux-musl-aarch64.zip \
            linux-musl-armv7.zip \
            linux-musl-i686.zip \
            linux-musl-riscv64.zip \
            linux-musl-x86_64.zip \
            windows-msvc-aarch64.zip \
            windows-msvc-x86_64.zip | sort)"
          actual="$(find . -maxdepth 1 -type f -name '*.zip' -print | sed 's#^\./##' | sort)"
          if [ "$actual" != "$expected" ]; then
            echo "Release artifact set does not match the expected files" >&2
            diff <(printf '%s\n' "$expected") <(printf '%s\n' "$actual") || true
            exit 1
          fi
      - name: Generate checksums
        working-directory: assets
        run: sha256sum -- *.zip > SHA256SUMS
      - name: Attest build provenance
        uses: actions/attest-build-provenance@0f67c3f4856b2e3261c31976d6725780e5e4c373 # v4.1.1
        with:
          subject-path: |
            assets/*.zip
            assets/SHA256SUMS
      - name: Publish
        uses: softprops/action-gh-release@3d0d9888cb7fd7b750713d6e236d1fcb99157228 # v3.0.2
        with:
          tag_name: ${{ github.ref_name }}
          fail_on_unmatched_files: true
          files: |
            assets/*.zip
            assets/SHA256SUMS
